# The pnpm workspace includes the yarn-era SDK (typescript/), whose sources
# import @ethersproject/* sub-packages without declaring them as direct
# dependencies, relying on a flat (hoisted) node_modules. Use a hoisted node
# linker so the SDK builds under pnpm the same way it does under yarn.
node-linker=hoisted
