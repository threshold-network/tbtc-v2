diff --git a/node_modules/electrum-client-js/src/socket/socket_client.js b/node_modules/electrum-client-js/src/socket/socket_client.js
index 1111111..2222222 100644
--- a/node_modules/electrum-client-js/src/socket/socket_client.js
+++ b/node_modules/electrum-client-js/src/socket/socket_client.js
@@ -74,10 +74,11 @@
     if (msg instanceof Array) {
       ; // don't support batch request
     } else {
       if (msg.id !== void 0) {
         this.response(msg)
       } else {
-        this.subscribe.emit(msg.method, msg.params)
+        // Notifications use EventEmitter on `events` (see example/subscribe.js).
+        this.events.emit(msg.method, msg.params)
       }
     }
   }
diff --git a/node_modules/electrum-client-js/src/electrum/client.js b/node_modules/electrum-client-js/src/electrum/client.js
index 3333333..4444444 100644
--- a/node_modules/electrum-client-js/src/electrum/client.js
+++ b/node_modules/electrum-client-js/src/electrum/client.js
@@ -18,15 +18,14 @@
         // Connect to Electrum Server.
         await super.connect()
 
-        // Get banner.
-        const banner = await this.server_banner()
-        console.log(banner)
-
-        // Negotiate protocol version.
+        // server.version must be first — Fulcrum/ElectrumX reject server.banner before identify.
         if (clientName && electrumProtocolVersion) {
           const version = await this.server_version(clientName, electrumProtocolVersion)
           console.log(`Negotiated version: [${version}]`)
         }
+
+        const banner = await this.server_banner()
+        console.log(banner)
       } catch (err) {
         throw new Error(`failed to connect to electrum server: [${err}]`)
       }
