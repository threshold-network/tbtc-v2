# Yarn 4 local state (not for zero-installs)
.yarn/install-state.gz

/external/
/typechain/

# Yarn Berry (nodeLinker: node-modules) — ignore local state, keep repo-managed dirs
.yarn/*
!.yarn/releases
!.yarn/plugins
!.yarn/sdks
!.yarn/versions
!.yarn/patches
.pnp.*
